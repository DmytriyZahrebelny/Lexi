services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: lexi
      POSTGRES_PASSWORD: lexi
      POSTGRES_DB: lexi
    ports:
      - "5432:5432"
    volumes:
      - lexi_pg_data:/var/lib/postgresql/data

volumes:
  lexi_pg_data:
